import { ChangeDecimalFormatPipe } from './change-decimal-format.pipe';

describe('ChangeDecimalFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDecimalFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
